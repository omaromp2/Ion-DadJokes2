import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonDadJokes2',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
